version: "3"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./node_modules:/tmp/app/node_modules
      - ./node_modules:/app/node_modules
    ports:
      - "3000:3000"
